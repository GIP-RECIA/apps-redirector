name: Tests

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  unit-tests:
    name: PHP ${{ matrix.php-version }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        php-version: ['8.3', latest]
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php-version }}
          extensions: curl, dom
          coverage: none

      - name: Install phpCAS
        run: composer install --working-dir=ci --no-interaction --prefer-dist

      - name: Verify phpCAS installation
        run: php -r "require 'ci/vendor/autoload.php'; exit(class_exists('phpCAS') ? 0 : 1);"

      - name: Lint source files
        run: |
          php -l index.php
          php -l resolution.php

      - name: Install synthetic CI configuration
        run: cp ci/conf/conf.inc.php conf/conf.inc.php

      - name: Lint CI configuration
        run: php -l conf/conf.inc.php

      - name: Run resolution unit tests
        run: php tests/resolution.php
